# Power Above All — рабочие заметки

- Пользователь выбрал английское название Power Above All. Папка переименована в C:/Users/USER/projects/power-above-all. Название не меняет язык интерфейса: он русский.
- Пользователь явно разрешил субагентов. Три параллельные задачи: styles.css (interface), simulation.js + tests (simulation), battle.js (battle). Родитель владеет app.js, index.html, документацией, интеграцией и браузерной проверкой.
- Пользователь хочет следить за прогрессом: STATUS.md + запускаемый START.cmd + локальные Git-коммиты проверенных этапов.
- Пользователь попросил постоянно использовать четыре исходных игровых референса; они сохранены в REFERENCES.md с адаптацией к 1789 году.
- Пользователь явно разрешил создать PRIVATE GitHub repository и загрузить проект. Предполагаемое имя power-above-all. Публиковать только проект, без файлов аккаунта или соседних каталогов; дождаться проверенного первого наброска перед push.
- Репозиторий создан: https://github.com/DvaGamer/power-above-all ; gh repo view подтвердил isPrivate:true, visibility:PRIVATE. origin добавлен, main инициализирован. Первый push пока в работе.
- Субагенты завершили свои задачи. Ядро прошло10 node:test проверок; родитель повторил их успешно. tests/browser-smoke.js прошёл в отдельной сессии power-above-all-check: 13 групп проверок, ошибок JS0, реальный полный бой выигран, в армию вернулось624 солдата после двух столкновений. Проверено ручное сохранение и точное восстановление автосохранения.
- Пользователь мог взаимодействовать с открытой headed-сессией power-above-all; не сбрасывать её состояние. Все автоматические проверки выполняются отдельно в power-above-all-check.
- Локальный сервер запущен через Windows Node на127.0.0.1:1789, exec session69549. Playwright доступен через cmd.exe /c npx --yes --package @playwright/cli playwright-cli. CLI run-code поддерживает --filename и ожидает JS-функцию async(page)=>{...}.
- Браузерные снимки desktop.png, economy.png, battle.png, mobile.png в output/playwright. Проверены визуально. Небольшой экран390×844 без горизонтального переполнения.
- Исправлены неточные подписи базовых доходов, зерна, стоимости набора и правил недовольства. Неактивный слой недовольства больше не показывает вводящую в заблуждение цветовую легенду.
- Мягкий баланс: без приказов выбор relief на неделе2 даёт победу на неделе8 (казна2057, зерно341, армия1200, недовольство36). Это учебный прототип; полноценная балансировка впереди.
- Реализация загружена в private GitHub main: commit7e4723d, remote origin https://github.com/DvaGamer/power-above-all.git . GitHub Actions33987434163 завершилсяsuccess (синтаксис и10 тестов ядра).
- START.cmd теперь вызывает launch.cjs: проверяет127.0.0.1:1789, при необходимости запускает скрытый Node-сервер и открывает браузер. Сам launcher --check и START.cmd выполнены успешно. Проверка file:// заблокирована политикой Playwright CLI, поэтому основной проверенный запуск — через localhost; обход блокировки не предпринимался.
- Финальная браузерная проверка:0 JS-ошибок; карта1440×960 и390×844 без переполнения; бой выигран, отступление и повторное сражение правильно уменьшают солдат и переходы. README, STATUS, CHANGELOG, REFERENCES доступны в GitHub.

- Пользователь подтвердил 1789 год, Великую французскую революцию; первоначальное предположение о 1830 годе отменено.
- Запрос: создать проект и простой запускаемый набросок. Людей в команде разработки нет.
- Unity не найден в стандартной папке. Первый набросок — автономный HTML/CSS/JavaScript, без сборщика и внешних зависимостей. Это проверка игрового цикла, не готовый Unity-проект. Ядро симуляции отделено от интерфейса.
- Проект: C:/Users/USER/projects/france-1789. Node v24.18.0 и npm/npx доступны через Windows cmd.exe из bash. В WSL нет rg и node; использовать find и Windows node.
- Исторический источник прочитан через qwen-web scrape: https://en.chateauversailles.fr/discover/history/key-dates/summoning-estates-general-1789 . Генеральные штаты открылись 5 мая 1789 года в Версале. Этот день — начало сценария.
- Карта будет схематичной: укрупнённые игровые области с историческими названиями, не точные границы провинций 1789 года. Числа экономики и сценарные столкновения вымышлены для прототипа.
- Первый цикл: выбрать область → принять решение → отправить армию/сыграть столкновение → завершить неделю → увидеть экономические последствия. Сохранение локальное.
- Для браузерной проверки прочитан C:/Users/USER/.codex/skills/playwright/SKILL.md; использовать Playwright CLI, снимки в output/playwright/.
